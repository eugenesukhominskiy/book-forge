version: "3.9"
services:
  database:
    image: postgres:latest
    environment:
      POSTGRES_DB: "book-forge-db"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
    ports:
      - "5432:5432"